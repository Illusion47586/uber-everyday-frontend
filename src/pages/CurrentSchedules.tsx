import React from "react";

import s from "../styles/pages/base_page.module.scss";

type Props = {};

const CurrentSchedules = (props: Props) => {
  return <div className={s.page}>CurrentSchedules</div>;
};

export default CurrentSchedules;
